<h2> ICE CREAM FLAVOURS </h2>
<div id="gallery">
<ul id="grid">
<% @products.each do |product| %>
   <li> <%= link_to image_tag(product.image), flavour_path(product.id), class:"icecream-photo", alt:product.name %> </li>
<% end %>
</ul>
</div>